using System;

namespace Array
{
    class Program
    {
        static void Main(string[] args)
        {
            //  1.)  To reference a particular element in an array,always specify the row first and column second.

            // Arrays are used to store multiple values in a single variable.To declare an array, we define the variable type with square brackets i.e: string[] cars;

           //   2.)  Declared arrays are allocated memory at compile time and their size is fixed, i.e.,it cannot be changed later.
        }
    }
}
